import * as vscode from "vscode";

/**
 * 拡張機能のアクティベーション
 */
export function activate(context: vscode.ExtensionContext): void {
  // TODO: 各コンポーネントの初期化と接続（タスク12で実装）
}

/**
 * 拡張機能のディアクティベーション
 */
export function deactivate(): void {
  // TODO: リソースのクリーンアップ（タスク12で実装）
}
